# Deepgram.CLI

Command-line interface for the Deepgram SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Deepgram.CLI --prerelease
```

## Usage

```bash
deepgram --help
deepgram api --help
```